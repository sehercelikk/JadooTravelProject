@using JadooTravel.Dtos.ServiceDtos
@model List<ResultServiceDto>
@{
    ViewData["Title"] = "ServiceList";
    Layout = "~/Views/AdminUI/Index.cshtml";
}

<div class="container">
    <br />
    <table class="table table-bordered">
        <tr>
            <td>#</td>
            <td>Icon</td>
            <td>Başlık</td>
            <td>Açıklama</td>
            <td>Sil</td>
            <td>Güncelle</td>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.IconUrl</td>
                <td>@item.Title</td>
                <td>@item.Description</td>
                <td><a href="/Service/DeleteService/@item.Id" class="btn btn-danger">Sil</a></td>
                <td><a href="/Service/UpdateService/@item.Id" class="btn btn-info">Güncelle</a></td>
            </tr>
        }
    </table>
    <a href="/Service/CreateService" class="btn btn-success">Kayıt Ekle</a>
</div>
